import type { NavItem } from "@/lib/nav-extensions";

/**
 * Phase 3 additions: events + sharing.
 * Merge by concatenation into your existing nav array,
 * preserving homepage header structure/styles.
 */
export const INARTACT_NAV_EXTENSIONS_PHASE3: NavItem[] = [
  { label: "Events", href: "/events" }
];
